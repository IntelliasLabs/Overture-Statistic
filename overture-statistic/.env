# Password for the 'elastic' user (at least 6 characters)
ELASTIC_PASSWORD=elastic

# Elastic token
ELASTIC_TOKEN=

# Password for the 'kibana_system' user (at least 6 characters)
KIBANA_PASSWORD=elastic

# Version of Elastic products
STACK_VERSION=8.17.0

# Set the cluster name
CLUSTER_NAME=overture-intellias

# Set to 'basic' or 'trial' to automatically start the 30-day trial
LICENSE=basic
#LICENSE=trial

# Port to expose Elasticsearch HTTP API to the host
ES_PORT=9200
#ES_PORT=127.0.0.1:9200

# Port to expose Kibana to the host
KIBANA_PORT=5601
#KIBANA_PORT=80

# Increase or decrease based on the available host memory (in bytes)
MEM_LIMIT=1073741824

# Project namespace (defaults to the current folder name if not set)
COMPOSE_PROJECT_NAME=overture-intellias


BATCH_ENABLED_JOBS=BuildingJob,addressJob,DivisionJob,DivisionAreaJob,DivisionBoundaryJob,WaterJob,InfrastructureJob,LandJob,LandUseJob,ConnectorJob,PlaceJob,landCoverJob,SegmentJob,bathymetryJob
BATCH_INPUT_HOST_PATH=<PATH to Overture DATA>
SERVER_PORT=8103
